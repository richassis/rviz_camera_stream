<library path="camera_display">
  <class
    name="rviz_camera_stream/CameraPub"
    type="rviz_camera_stream::RenderTextureDisplay"
    base_class_type="rviz_common::Display"
  >
    <description>
      Renders the main 3D scene to a texture and publishes it as a ROS Image message.
      Same as standard RViz camera, with addition of the ability to
      publish the rendered image to the Image topic.
    </description>
  </class>
</library>
